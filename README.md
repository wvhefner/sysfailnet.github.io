# Jeremy Hefner — Academic Website

This is a framework-free static website. It has no npm dependencies, build
process, tracking, analytics, or JavaScript.

Upload `index.html`, `style.css`, and the `images` folder to any static web
host. To preview locally, open `index.html` in a browser.
